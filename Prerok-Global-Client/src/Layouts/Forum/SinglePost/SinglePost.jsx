const SinglePost = () => {
  return (
    <div>
      {/*Single post page will contain single post along with an image
        Create separate components in component folder for like and comment.
        */}
    </div>
  );
};

export default SinglePost;
