import useUser from "../../../../hooks/useUser";

const AllUsersPage = () => {
    const user = useUser();
    console.log(user);
    return (
        <div>
            
        </div>
    );
};

export default AllUsersPage;